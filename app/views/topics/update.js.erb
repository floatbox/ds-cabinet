<% if @topic.errors.any? %>
  $('#edit_topic_<%= @topic.id %>').closest('.modal-dialog').replaceWith('<%=j render 'form' %>');
<% else %>
  window.location.replace('<%= topic_url(@topic) %>')
<% end %>