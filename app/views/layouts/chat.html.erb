<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Деловой Ассистент</title>

  <!-- Bootstrap -->
  <link href="/chat/css/bootstrap.css" rel="stylesheet">
  <link href="/chat/css/style.css" rel="stylesheet">
  <%= stylesheet_link_tag "chat/application" %>

  <%= javascript_include_tag "chat/application" %>
  <script type="text/javascript" src="<%= Rails.configuration.widget_script_url %>"></script>

  <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
  <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
  <!--[if lt IE 9]>
  <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
  <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
  <![endif]-->

  <%= csrf_meta_tags %>

  <script>
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
    (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
    m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
    })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

    ga('create', 'UA-51418540-1', 'legko.li');
    ga('send', 'pageview');
  </script>
</head>
<body>
  <div class="container">
    <div class="header">
      <a href="/"><img src="/chat/images/header-logo.png" height="58" width="75" alt="" class="header-logo pull-left"></a>

      <%= render partial: 'notifications/panel', object: current_user.notifications.unread.page(0).per(5), as: :notifications %>

      <% if !current_user.concierge? || params[:user_id].present? %>
        <%= render partial: 'search_bar' %>
      <% end %>

      <div class="header-logged">
        <img src="/images/avatar_dummy.png" height="32" width="32" alt="" class="ava">
        <p>
          <%= link_to 'Выйти', session_path('current_user'), method: :delete, class: 'logout' %>
          <br />
          <% if current_user.siebel.full_name == 'Не определено Не определено' %>
            <%= link_to 'Профиль', edit_user_path(current_user), class: 'js-show-modal-window', data: { target: '#js-modal' } %>
          <% else %>
            <%= link_to current_user.siebel.full_name, edit_user_path(current_user), class: 'js-show-modal-window', data: { target: '#js-modal' } %>
          <% end %>
        </p>
      </div>
    </div>

    <%= yield %>

    <div class="footer">
        <p class="footer-copy pull-left"> © 2014 ЗАО «Деловая среда»</p>
        <p class="footer-contacts pull-right">
          +7-800-100-90-80<br>
          <a href="mailto:support@dasreda.ru">support@dasreda.ru</a>
        </p>
      </div>
    </div>

    <div id="js-modal" class="popup"></div>

    <!-- fancybox -->
    <link rel="stylesheet" href="/chat/js/fancybox/jquery.fancybox.css">
    <script src="/chat/js/fancybox/jquery.fancybox.pack.js"></script>
    <!-- custom scrollbar -->
    <link rel="stylesheet" href="/chat/js/customscrollbar/style.css">
    <script src="/chat/js/customscrollbar/baron.full.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="/chat/js/bootstrap.min.js"></script>
    <script src="/chat/js/call.js"></script>

    <%= render 'update_notifications' %>
  </body>
  </html>
