var panel = $('#js-notifications');
if (panel.data('count') != <%= @notifications.total_count %>) {
  panel.replaceWith('<%=j render partial: 'notifications/panel', object: @notifications, as: :notifications %>');
  $('#js-notifications').find('.js-count').effect('highlight', 1500);
}